#cloud-config
hostname: dissect-centos-stream-10
fqdn: dissect-centos-stream-10.smoketest

users:
  - default
  - name: user
    sudo: ALL=(ALL) NOPASSWD:ALL
    lock_passwd: false
    plain_text_passwd: password

ssh_pwauth: true

write_files:
  - path: /etc/dissect-smoketest/provisioned
    content: |
      template=centos-stream-10
      lifecycle=pinned
    permissions: '0644'
