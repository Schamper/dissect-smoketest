# Debian 13 preseed: ecryptfs encrypted home via user-setup/encrypt-home

d-i debian-installer/locale string en_US.UTF-8
d-i console-setup/ask_detect boolean false
d-i keyboard-configuration/xkb-keymap select us

d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string ecryptfs
d-i netcfg/get_domain string local
d-i netcfg/hostname string ecryptfs

d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

d-i passwd/root-login boolean true
d-i passwd/root-password password password
d-i passwd/root-password-again password password
d-i user-setup/allow-password-weak boolean true
d-i passwd/user-fullname string user
d-i passwd/username string user
d-i passwd/user-password password password
d-i passwd/user-password-again password password
d-i user-setup/encrypt-home boolean true

d-i clock-setup/utc boolean true
d-i time/zone string UTC
d-i clock-setup/ntp boolean true

d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman-basicfilesystems/no_swap boolean false
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

d-i apt-setup/use_mirror boolean true
d-i apt-setup/services-select multiselect security, updates
tasksel tasksel/first multiselect standard
d-i pkgsel/include string openssh-server ecryptfs-utils sudo
d-i pkgsel/upgrade select none
popularity-contest popularity-contest/participate boolean false

d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true
d-i grub-installer/bootdev string default

d-i preseed/late_command string \
    mkdir -p /target/etc/dissect-smoketest; \
    printf 'template=ecryptfs\nlifecycle=corpus\n' > /target/etc/dissect-smoketest/provisioned; \
    in-target usermod -aG sudo user; \
    printf 'user ALL=(ALL) NOPASSWD:ALL\n' > /target/etc/sudoers.d/90-user-nopasswd; \
    chmod 440 /target/etc/sudoers.d/90-user-nopasswd

d-i finish-install/reboot_in_progress note
